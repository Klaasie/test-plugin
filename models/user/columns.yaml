# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true

    username:
        label: Username
        searchable: true

    roles_count:
        label: Roles
        relation: roles_count
        valueFrom: count
        default: 0

    created_at:
        label: Created at
        type: datetime
        format: Y-m-d H:i:s
